import { AppShell } from '@/components/shell';
import { ComptabiliteScreen } from '@/components/finance';

export default function Page() {
  return (
    <AppShell>
      <ComptabiliteScreen />
    </AppShell>
  );
}
