import { AppShell } from '@/components/shell';
import { ExpenseForm } from '@/components/finance';

export default function Page() {
  return (
    <AppShell>
      <ExpenseForm />
    </AppShell>
  );
}
